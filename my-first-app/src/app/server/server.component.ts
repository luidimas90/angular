import { Component } from '@angular/core';

@Component({//decorator
    //All we put here is stored as metadata for this class
    selector: 'app-server', //must be a unique selector
    templateUrl: './server.component.html',

}) 
export class ServerComponent{

}